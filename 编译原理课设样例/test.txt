// 这是一个单行注释
/* 这是一个
   多行注释
   测试 */

int main() {
    // 变量声明和初始化
    int a = 10;
    int b = 20;
    
    // 测试if语句和算术运算
    if (a < b) {
        int c = a + b;
        a = c * 2;
    }
    
    // 测试while循环
    while (a > 0) {
        b = b + 1;
        a = a - 1;
    }
    
    return 0;
} 